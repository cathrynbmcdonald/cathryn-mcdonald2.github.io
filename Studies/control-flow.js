/**
 * 
 * CONTROL FLOW:
 * 
 * 0: Control Flow is 
 * 
 * 1. if: To start a conditional block of code, you first use an if statement
 * 
 * 2. else-if: You can then chain together any number of else-if statements to your first if statement
 * 
 * 3. else: You can add a default else statement to the end of the chain
 * 
 * 4. switch: The switch statement evaluates an input expression, matches the expression's value to one of the case 
 * clauses, and then executes statements associated with that case. A break statement ends the code block.
 */

// 1 // if, else-if, else:

var condition = 'red';

if (condition === 'blue' {
    console.log('blue');
} else if (condition === 'red') {
    console.log('red');
} else {
    console.log('neither');
}

// 2 // switch

